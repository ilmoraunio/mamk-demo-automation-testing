dependencies:
  pre:
    - npm install -g cypress-cli
    - npm install -g forever
    - cypress install
test:
  override:
    - npm test
    - forever start server.js
    - cypress run # requires CYPRESS_CI_KEY (https://docs.cypress.io/docs/continuous-integration#section-acquire-a-cypress-secret-key)